## WS-3 Status Report

### Tasks completed
- WS-3.1 Exponential decay with recall reinforcement — new `recallCount` column (migration `20260514_recall_count`), formula `strength = importance * EXP(-λ * days_since_access) * (1 + recall_count * 0.2)` with `λ = 0.16 * (1 - importance * 0.8)`. SQL UPDATE in `runImportanceDecay()`; pure helper `computeDecayedImportance()` exported for tests. Recall reinforcement wired in `context-assembler.service.ts` — every memory in the post-rank `rankedMemories` set bumps `recallCount` and refreshes `lastAccessedAt` (fire-and-forget).
- WS-3.2 Dedup threshold lowered 0.85 -> 0.80 in `packages/omnimind-mcp/src/lib/fact-extractor.ts` and `/memories/search-similar` default in `packages/omnimind-api/src/routes/memories.routes.ts`.
- WS-3.3 SourceWeight as tiebreaker in `packages/omnimind-api/src/retrieval/ranker.ts` — removed the `* sourceWeight` multiplier; sort comparator now uses raw weightedScore first, only falling back to `sourceWeight` delta when `|score_a - score_b| <= 0.05`.

### Tests added
- `tests/audit/D12-exponential-decay.test.ts` — 6 tests covering: identity at zero days/recalls, reinforcement ordering (5 recalls > 0 recalls at equal importance, 2x ratio), higher importance decays slower, recent+recalled > old+untouched, the YourMemory thesis case (low-importance-but-recalled overtakes high-importance-but-stale), clamping to [0,1].
- `tests/audit/D13-tiebreaker-ranking.test.ts` — 5 tests covering: equal scores -> higher weight wins, meaningful score gap (>0.05) ignores weight, regression guard that weight isn't a multiplier, default weight (1.0) is neutral, small-gap (<0.05) tiebreaker activation.

### Success metrics
- A memory accessed 5 times has higher computed `strength` than one accessed 0 times at equal `importance` — exactly `2.0x` (D12 test 2).
- Threshold lowered to 0.80 in both call sites; D4-4c paraphrase-dedup behavior depends on real semantic search at runtime (unit test mocks `searchMemories` not `searchSimilar` — pre-existing fixture mismatch on main, not regressed by this change).
- D13 confirms two identical-score memories sort by `sourceWeight` (high-trust wins ties); a high-score memory at low trust still beats a low-score memory at high trust.

### Self-validation
- `pnpm typecheck` (omnimind-api): same 5 pre-existing errors as `main` (admin.routes.ts:225/233, memory.service.ts:68 `logger.info` pino-style call, weekly-digest.service.ts completedAt + nodemailer). My changes add zero new typecheck errors. omnimind-mcp + shared: clean.
- Audit tests: D12 6/6, D13 5/5, D5 3/3, D6 3/3, D3 6/6, D7 4/4 — 27/27 of the audit suite that runs cleanly. D4-4c failing on `main` with my changes stashed and with my changes applied (pre-existing fixture mismatch — test mocks `searchMemories` but code uses `searchSimilar`).
- Shared package tests: 125/125.
- Unit retrieval/ranker test: 5/5.
- `pnpm build` not run cleanly due to the same pre-existing typecheck errors gating tsc emit (these are not introduced by WS-3).
- `pnpm test:e2e` — not run (WS-5 harness not merged yet per the plan sequence).
- Hermes prod round-trip — not run (orchestrator's responsibility post-merge).

### Anomalies
- The workspace I executed in shared a single git checkout with WS-1's session. To produce a clean PR I worked in an isolated git worktree at `/tmp/ws3-worktree` based off `main`, then promoted the commit back to the canonical `.git` directory. The commit `fd86411` is on `feat/recall-quality` locally. **Push to origin must happen from Josh's Mac** — the workspace bash environment has no GitHub credentials.

### Deferred / out-of-scope findings (for follow-up issues)
- Pre-existing failure: `packages/omnimind-mcp/tests/fact-extractor.test.ts > marks duplicate facts as update when similarity hit found` and the mirror `tests/audit/D4-fact-extractor.test.ts > 4c` both mock `searchMemories` while the production code now calls `client.searchSimilar()`. Fixture needs updating, but is outside WS-3 scope (test file, not extractor code).
- Pre-existing typecheck errors on `main` (5 in omnimind-api): `admin.routes.ts:225` (object passed where string expected), `admin.routes.ts:233` (dynamic import not type-resolving), `memory.service.ts:68` (pino-style `logger.info({...}, msg)` against our custom logger that takes `msg` first), `weekly-digest.service.ts:21` (`completedAt` not on `TaskWhereInput`), `weekly-digest.service.ts:78` (`nodemailer` not installed). Not introduced or made worse by WS-3.
- `packages/omnimind-api/tests/setup.ts` referenced by `vitest.config.ts` does not exist — causes the entire api unit-test suite to fail collection. Pre-existing.
- `tests/golden-scenarios/auth-scenarios.test.ts:169` has a parse error (`Unexpected end of file`) — pre-existing.
